# 视频格式转换器 · 开发计划

> 面试小测试项目：一个基于 ffmpeg 命令行、带信息展示与缩略图预览的桌面视频格式转换器。

***

## 一、需求理解

原始需求（面试官给出）：

1. 加载视频信息并显示；
2. 获取并显示视频缩略图预览；
3. 视频转换通过 ffmpeg 命令行完成（不强制接入 ffmpeg API）；
4. 自行做一些合理优化；
5. 完成后生成会话总结 MD、提交 GitHub（回链给 HR）。

结合岗位介绍（Windows 桌面应用为主的 AI 应用开发实习，技术栈点名 Tauri / Electron / TypeScript / C# / Python），本项目定位为：**一款轻量、跨平台（Windows 优先，可平滑移植 macOS）的桌面视频转换工具**。

***

## 二、技术选型与理由

| 层    | 选型                                                    | 为什么用这个                                                                                                                                                                            |
| ---- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 桌面框架 | **Tauri 2.0（Rust）**                                   | ① 岗位要求直接点名 Tauri；② 打包体积小（约 5–10MB，对比 Electron 上百 MB），启动快、内存低，契合桌面工具定位；③ Rust 后端可高效 `spawn` ffmpeg/ffprobe 子进程并**流式读取 stdout** 获取实时进度，天然适合 CLI 包装；④ 跨平台（Windows/macOS），呼应岗位“多平台”方向 |
| 前端   | **React 18 + TypeScript + Vite**                      | 组件化承载“文件队列 + 预览 + 设置 + 进度”多处联动 UI；TS 提供类型安全，便于与 Rust 侧 IPC 契约对齐；Vite 开发热更新快                                                                                                       |
| 样式   | **Tailwind CSS**                                      | 快速搭建现代化 UI，暗色主题统一观感，减少手写 CSS                                                                                                                                                      |
| 状态管理 | **Zustand**                                           | 轻量、无样板代码，管理转换队列/进度/设置等跨组件状态                                                                                                                                                       |
| 视频处理 | **ffmpeg / ffprobe 命令行**                              | 需求明确指定命令行；ffmpeg 生态成熟，格式覆盖全；通过 Tauri Command 由 Rust 侧统一调用                                                                                                                         |
| 信息解析 | **ffprobe** **`-show_format -show_streams -of json`** | 一条命令拿到时长、分辨率、编码、码率、帧率、大小等结构化 JSON，前端解析渲染                                                                                                                                          |
| 缩略图  | **ffmpeg** **`-ss <t> -vframes 1`** **抽帧**            | 指定时间点抽出单帧生成 JPG/PNG，简单可靠，可做多帧预览                                                                                                                                                   |

> 备选说明：若团队更偏 C# 生态，可用 **WPF/WinUI3** 重写 UI 层 + `System.Diagnostics.Process` 调用 ffmpeg；本方案选择 Tauri 是因为更贴合岗位列出的现代跨平台方向、也更适合用 AI 编程工具（Cursor/Claude Code）快速迭代。

***

## 三、系统架构

```
┌─────────────────────────────────────────────┐
│              前端 (React + TS)               │
│  文件导入 / 信息卡片 / 缩略图 / 设置 / 队列    │
└──────────────────┬──────────────────────────┘
                   │  Tauri IPC (invoke)
┌──────────────────▼──────────────────────────┐
│          后端 (Rust · Tauri Core)            │
│  · 文件选择/拖拽落盘                          │
│  · Command: get_video_info → 调用 ffprobe    │
│  · Command: get_thumbnail   → 调用 ffmpeg    │
│  · Command: convert_video   → 调用 ffmpeg    │
│  · 事件: progress / done / error (emit)      │
└──────────────────┬──────────────────────────┘
                   │  std::process::Command
        ┌──────────┴──────────┐
        │   ffmpeg / ffprobe  │  (命令行可执行文件)
        └─────────────────────┘
```

- 前端不直接接触进程，全部通过 Tauri `invoke` 调用后端 Command，后端通过 `emit` 把进度/完成/错误事件推回前端。

- ffmpeg 通过**捆绑二进制（sidecar）或系统 PATH 检测**两种方式定位，首次运行可选“自动下载内置 ffmpeg”或“手动指定路径”。

***

## 四、模块划分

| #  | 模块      | 职责                        | 关键技术点                         |
| -- | ------- | ------------------------- | ----------------------------- |
| M1 | 文件导入模块  | 拖拽/点击选择视频，多文件支持、格式校验      | Tauri dialog、拖拽事件、文件元信息       |
| M2 | 视频信息模块  | 展示时长/分辨率/编码/码率/帧率/大小      | ffprobe JSON 解析、信息卡片渲染        |
| M3 | 缩略图模块   | 多时间点抽帧、当前帧预览、进度条时间轴绑定     | ffmpeg `-ss` 抽帧、Base64/临时文件显示 |
| M4 | 转换设置模块  | 目标格式、预设、分辨率、码率、编码器、音视频参数  | 预设方案（设备/平台）、参数校验              |
| M5 | 转换引擎模块  | 构造并执行 ffmpeg 命令、解析进度、错误处理 | Rust Command、stdout 解析、参数转义   |
| M6 | 队列与进度模块 | 批处理队列、单文件进度、总进度、取消/重试     | Zustand、进度事件、并发控制(串行)         |
| M7 | 输出与历史模块 | 输出目录选择、完成后提示打开目录、历史记录     | 本地存储、资源定位                     |
| M8 | 设置模块    | ffmpeg 路径、默认输出目录、主题、语言    | 持久化配置                         |

***

## 五、UI 布局设计

采用经典“三栏 + 底部进度”的桌面工具布局：

```
┌────────────────────────────────────────────────────────┐
│ 顶部栏：Logo/名称         [设置] [历史]                  │
├───────────┬──────────────────────────┬─────────────────┤
│           │                          │  转换设置        │
│  文件列表  │   视频预览区（缩略图大图） │  · 目标格式       │
│  +添加     │                          │  · 预设方案       │
│  ├ file1   │                          │  · 分辨率         │
│  └ file2   │  信息卡片（时长/分辨率/    │  · 码率/编码器    │
│  (队列)    │   编码/码率/帧率/大小）     │  · 音频参数       │
│           │                          │                  │
├───────────┴──────────────────────────┴─────────────────┤
│  底部：总进度条  文件x/y  [开始转换] [取消]              │
└────────────────────────────────────────────────────────┘
```

布局语义：左栏“选什么”→ 中栏“看效果”→ 右栏“怎么转”→ 底部“进度与执行”。

***

## 六、开发步骤（里程碑）

1. **脚手架**：`npm create tauri-app`（React-TS 模板）搭建基础工程，跑通空壳窗口。
2. **M1+M2**：实现文件选择与 `get_video_info`，前端信息卡片展示。
3. **M3**：实现 `get_thumbnail`，中栏缩略图预览 + 时间轴抽帧。
4. **M4+M5**：实现设置面板与 `convert_video`，单文件可成功转出。
5. **M6**：加入队列、实时进度解析、取消/重试。
6. **M7+M8**：输出目录定位、历史记录、设置持久化。
7. **优化与打磨**：预设方案、错误自检（ffmpeg 缺失提示）、暗色主题、空状态/加载态。
8. **打包发布**：`tauri build` 生成 Windows 安装包（含捆绑 ffmpeg），验证可分发。
9. **文档与提交**：生成会话总结 MD、补充 README、提交 GitHub 并回链。

***

## 七、优化点（关键要点）

- 拖拽多文件导入 + 串行队列批量转换。

- 实时进度解析（`-progress pipe:1`），而非粗略的“运行中”状态。

- 预设方案一键选参（如“兼容 H.264 / Web 高清 / 提取音频 / GIF 动图”）。

- ffmpeg 缺失时优雅降级：引导下载或指定路径。

- 转换完成后一键“打开所在目录”。

- 转换历史本地记录，方便复用参数。

- 抽帧预览 + 时间轴联动，增强体验。

***

## 八、目录结构（规划）

```
视频格式转换器/
├─ PLAN.md                    # 本计划文档
├─ demo/index.html            # 前端布局 Demo（可双击浏览器预览）
├─ src/                       # 前端源码
│  ├─ components/             # 预览/信息卡/设置面板/队列等组件
│  ├─ store/                  # Zustand 状态
│  └─ api/                    # Tauri invoke 封装
├─ src-tauri/                 # Rust 后端
│  ├─ src/                    # Command 实现（info/thumbnail/convert）
│  └─ tauri.conf.json         # 配置、bundler、sidecar 声明
└─ SUMMARY.md                 # 会话总结（完成后生成）
```

***

## 九、提交 GitHub 计划

1. 本地 `git init`，编写 `.gitignore`（忽略 node\_modules、target、dist、临时抽帧图片）。
2. 初始化远端仓库，提交计划文档 → Demo → 源码 → 打包产物说明。
3. 提交高质量 commit（每个里程碑一次），README 附截图与使用说明。
4. 完成后将仓库链接回给 HR / BossHR。

